package gui;

import javax.swing.JLabel;

/*
 * This class is the JLabel for the main gui class to use. Which will
 * be used to display the card images onto the gui application.
 * Take away variable declaration from the main gui app and placed
 * into its own separate class for design purposes.
 */
public class VisualDisplay extends JLabel
{
	/* Get a default JLabel with no image */
	public VisualDisplay()
	{
		super();
	}
	
	/* May need to add another ctor if want app to begin with
	 * preset images.
	 */
}
